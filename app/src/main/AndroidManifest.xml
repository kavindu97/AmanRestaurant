<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mad_kavindu">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.adminHome_pg"></activity>
        <activity android:name=".activities.admin_update_it" />
        <activity android:name=".activities.HomeScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.Admin_AddPramotionItem" />
        <activity android:name=".activities.Admin_PramotionList" />
        <activity android:name=".activities.Admin_EditPramotionItem" />
        <activity android:name=".activities.Customer_PramotionList" />
    </application>

</manifest>